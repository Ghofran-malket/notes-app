{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit Note</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Update Note</button>
    {{ form_end(form) }}

    <a href="{{ path('note_all') }}" class="btn btn-secondary">Back to Notes</a>
{% endblock %}